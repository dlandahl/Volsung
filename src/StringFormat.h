#pragma once

#include <string>
#include <vector>

#include "Yggdrasil.h"

namespace sf
{
	bool             starts_with(str, str);
	std::vector<str> split_by(str, char);
	str              remove_spaces(str);
}